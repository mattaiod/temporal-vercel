- name: allowed-queries
  definition:
    queries:
      - name: getNake
        query: |
          query MyQuery {
            testt(where: {nake:{_eq:22}}) {
              nake
            }
          }
